import styles from "./Header.module.css";

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>Todo List</h1>
      <button>color</button>
    </header>
  );
};

export default Header;
